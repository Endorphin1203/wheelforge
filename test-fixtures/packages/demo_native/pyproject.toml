[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "demo-native"
version = "1.0.0"
description = "Deterministic platform-specific Wheel fixture"
requires-python = ">=3.9"
