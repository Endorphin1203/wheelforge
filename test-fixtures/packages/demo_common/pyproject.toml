[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "demo-common"
version = "1.2.3"
description = "Deterministic pure-Python Wheel fixture"
requires-python = ">=3.9"
